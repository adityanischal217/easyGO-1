<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.easygo">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/easygo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".signup" />
        <activity android:name=".MEDICALMAP" />
        <activity android:name=".CABMAP" />
        <activity android:name=".atmmap" />
        <activity android:name=".login" />
        ...
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        ...
        <activity android:name=".xyz" />
        <activity android:name=".facility" />
        <activity android:name=".avfacility" />
        <activity android:name=".flight" />
        <activity android:name=".hotels" />
        <activity android:name=".livestatus" />
        <activity android:name=".registration" />
        <activity
            android:name=".home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" >

    </activity>
        <activity android:name=".lost" >
            </activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDybbgSbIzi1dYLhzhqPVuJJpVlSht9978" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".deal" />
        <activity android:name=".hungry" />
        <activity android:name=".shopping" />
        <activity android:name=".abc"> </activity>
        <activity android:name=".feedback">

        </activity>
        <activity android:name=".map2d"> </activity>
    </application>
</manifest>